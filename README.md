# freeipa-ipsilon-prototype
Ansible and Vagrant files to help with prototyping an environment to test SSO.

Requirements:
- Ansible
- Vagrant
  - vagrant-hostmanager (plugin)


Running:
```
vagrant up --no-parallel
```
